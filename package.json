{
  "name": "typique-umbrella",
  "private": true,
  "version": "1.0.0",
  "description": "Bundler-agnostic, zero-runtime CSS-in-TS — powered by a TypeScript plugin",
  "license": "ISC",
  "author": "Aleksei Berezkin",
  "scripts": {
    "build": "npm run build:bin & npm run build:cjs & npm run build:esm & npm run build:plugin & wait",
    "build:bin": "(cd packages/typique/src/bin && tsc && chmod +x ../../bin/typique.mjs)",
    "build:cjs": "(cd packages/typique/src && tsc --project tsconfig.cjs.json)",
    "build:esm": "(cd packages/typique/src && tsc)",
    "build:plugin": "(cd packages/typique/src/ts-plugin && tsc)",
    "clean:build": "npm run clean && npm run build",
    "clean": "npm run clean:package && npm run clean:log && npm run clean:output && npm run clean:per-file",
    "clean:package": "(cd packages/typique && rm -rf bin && rm -rf cjs && rm -rf esm && rm -rf ts-plugin)",
    "clean:log": "find test-* -name '*.log' | xargs rm",
    "clean:output": "find test-* -name 'typique-output.css' | xargs rm",
    "clean:per-file": "rm -f test-server/per-file/*.css ; rm -f test-server/per-file/b.ts",
    "test": "npm run test:util && npm run test:plugin:unit && npm run test:server:unit && npm run test:server && npm run test:bin",
    "test:util": "node --no-warnings packages/typique/src/util/test/index.test.mts",
    "test:plugin:unit": "npm run build && node packages/typique/ts-plugin/pluginUnit.test.js",
    "test:server:unit": "node test-server/serverUnit.test.ts",
    "test:server": "npm run test:server:old && npm run test:server:new",
    "test:server:old": "npm run build && node test-server/server.old.test.ts",
    "test:server:new": "npm run build && node test-server/server.new.test.ts",
    "test:bin": "npm run build && node test-bin/bin.test.ts",
    "demos:build": "node demos/build.mts"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "typescript": "^5.8.3",
    "test-util": "file:./packages/test-util",
    "typique": "file:./packages/typique"
  }
}
