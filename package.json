{
  "name": "typique",
  "version": "0.0.0",
  "description": "Zero-runtime CSS-in-TS library working as a TypeScript Plugin",
  "license": "ISC",
  "author": "Aleksei Berezkin",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc && npm run build@plugin",
    "build@plugin": "(cd ts-plugin && tsc)",
    "clean": "npm run clean@js && npm run clean@log && npm run clean@output && npm run clean@tsbuildinfo",
    "clean@js": "rm -f index.js && rm -f index.d.ts && rm -f ts-plugin/*.js",
    "clean@log": "rm -f test/tsserver-*.log && rm -f test/ti-*.log",
    "clean@output": "rm -f test/*/typique-output.css",
    "clean@tsbuildinfo": "rm -f tsconfig.tsbuildinfo && rm -f ts-plugin/tsconfig.tsbuildinfo",
    "test": "npm run test@plugin && npm run test@server",
    "test@plugin": "npm run build@plugin && node ts-plugin/index.test.js",
    "test@server": "npm run build@plugin && node test/server.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "typique-ts-plugin-dev": "file:./ts-plugin",
    "typescript": "^5.8.3",
    "uvu": "^0.5.6"
  }
}
