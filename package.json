{
  "name": "typique",
  "version": "0.0.0",
  "description": "Bundler-agnostic, zero-runtime CSS-in-TS — powered by a TypeScript plugin",
  "license": "ISC",
  "author": "Aleksei Berezkin",
  "main": "index.js",
  "module": "index.mjs",
  "types": "index.d.ts",
  "sideEffects": false,
  "scripts": {
    "build": "npm run build@root && npm run build@plugin",
    "build@root": "tsc && mv index.js index.mjs && tsc --project tsconfig.cjs.json",
    "build@plugin": "(cd ts-plugin && tsc)",
    "clean": "npm run clean@compiled && npm run clean@log && npm run clean@output",
    "clean@compiled": "rm -f index.js && rm -f index.mjs && rm -f index.d.ts && rm -f ts-plugin/*.js && rm -f tsconfig.tsbuildinfo && rm -f ts-plugin/tsconfig.tsbuildinfo",
    "clean@log": "rm -f test/tsserver-*.log && rm -f test/ti-*.log",
    "clean@output": "rm -f test/*/typique-output.css",
    "test": "npm run plugin@test && npm run server@test",
    "plugin@test": "npm run build@plugin && node ts-plugin/index.test.js",
    "server@test": "npm run build@plugin && node test/server.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "typique-ts-plugin-dev": "file:./ts-plugin",
    "typescript": "^5.8.3",
    "uvu": "^0.5.6"
  }
}
