{
  "name": "typique",
  "version": "0.0.0",
  "description": "Bundler-agnostic, zero-runtime CSS-in-TS — powered by a TypeScript plugin",
  "license": "ISC",
  "author": "Aleksei Berezkin",
  "main": "index.js",
  "module": "index.mjs",
  "types": "index.d.ts",
  "sideEffects": false,
  "bin": {
    "typique": "./bin/typique.mjs"
  },
  "scripts": {
    "build": "npm run build@root && npm run build@bin && npm run build@plugin",
    "build@bin": "(cd bin && tsc && chmod +x typique.mjs)",
    "build@root": "tsc && mv index.js index.mjs && tsc --project tsconfig.cjs.json",
    "build@plugin": "(cd ts-plugin && tsc)",
    "clean": "npm run clean@bin && npm run clean@root && npm run clean@plugin && npm run clean@log && npm run clean@output && npm run clean@per-file",
    "clean@bin": "rm -f bin/*.js && rm -f bin/*.mjs && rm -f bin/*.d.ts",
    "clean@root": "rm -f index.js && rm -f index.mjs && rm -f index.d.ts && rm -f tsconfig.tsbuildinfo",
    "clean@plugin": "rm -f ts-plugin/*.js && rm -f ts-plugin/tsconfig.tsbuildinfo",
    "clean@log": "rm -f test/tsserver-*.log && rm -f test/ti-*.log",
    "clean@output": "rm -f test/*/typique-output.css",
    "clean@per-file": "rm -f test/per-file/*.css ; rm -f test/per-file/b.ts",
    "test": "npm run test@unit && npm run test@serverUnit && npm run test@server && npm run test@bin",
    "test@unit": "npm run build@plugin && node ts-plugin/pluginUnit.test.js",
    "test@serverUnit": "node test-server/serverUnit.test.ts",
    "test@server": "npm run build@plugin && node test-server/server.test.ts",
    "test@bin": "npm run build@bin && node test-bin/bin.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "typescript": "^5.8.3",
    "typique-ts-plugin-dev": "file:./ts-plugin"
  },
  "dependencies": {
    "csstype": "^3.1.3",
    "minimatch": "^10.1.1"
  }
}
