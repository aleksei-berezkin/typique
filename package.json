{
  "name": "typique-umbrella",
  "private": true,
  "version": "1.0.0",
  "description": "Bundler-agnostic, zero-runtime CSS-in-TS — powered by a TypeScript plugin",
  "license": "ISC",
  "author": "Aleksei Berezkin",
  "scripts": {
    "build": "npm run build:bin && npm run build:root && npm run build:plugin",
    "build:bin": "(cd packages/typique/bin && tsc && chmod +x typique.mjs)",
    "build:root": "(cd packages/typique && tsc && mv index.js index.mjs && mv util/index.js util/index.mjs && tsc --project tsconfig.cjs.json)",
    "build:plugin": "(cd packages/typique/ts-plugin && tsc)",
    "clean:build": "npm run clean && npm run build",
    "clean": "npm run clean:bin && npm run clean:root && npm run clean:plugin && npm run clean:log && npm run clean:output && npm run clean:per-file",
    "clean:bin": "rm -f bin/*.js && rm -f bin/*.mjs && rm -f bin/*.d.ts && rm -f *.tsbuildinfo",
    "clean:root": "(cd packages/typique && rm -f index.js && rm -f index.mjs && rm -f index.d.ts && rm -f *.tsbuildinfo)",
    "clean:plugin": "(cd packages/typique/ts-plugin && rm -f *.js && rm -f *.tsbuildinfo)",
    "clean:log": "rm -f test-server/tsserver-*.log && rm -f test-server/ti-*.log && rm -f test-bin/*/*.log",
    "clean:output": "rm -f test-server/*/typique-output.css && rm -f test-bin/*/typique-output.css",
    "clean:per-file": "rm -f test-server/per-file/*.css ; rm -f test-server/per-file/b.ts",
    "test": "npm run test:root && npm run test:unit && npm run test:serverUnit && npm run test:server && npm run test:bin",
    "test:root": "npm run build:root && node --no-warnings packages/typique/util/index.test.mts",
    "test:unit": "npm run build:plugin && node packages/typique/ts-plugin/pluginUnit.test.js",
    "test:serverUnit": "node test-server/serverUnit.test.ts",
    "test:server": "npm run test:server:old && npm run test:server:new",
    "test:server:old": "npm run build:plugin && node test-server/server.old.test.ts",
    "test:server:new": "npm run build:plugin && node test-server/server.new.test.ts",
    "test:bin": "npm run build:bin && node test-bin/bin.test.ts",
    "demos:build": "node demos/build.mts"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "typescript": "^5.8.3",
    "typique": "file:./packages/typique"
  }
}
